import { PlaceholderPage } from "@/components/Placeholder";
import { placeholderPages } from "@/lib/avai-mock-data";

export default function PapersPage() {
  return <PlaceholderPage {...placeholderPages.papers} />;
}
